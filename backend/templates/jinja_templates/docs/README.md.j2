{# README.md Documentation Template #}
# {{ project_name }}

{{ project_description }}

## Getting Started

### Prerequisites

- Node.js 18+
- SAP CAP SDK (`@sap/cds-dk`)
- SAP BTP account (for deployment)

### Installation

```bash
npm install
```

### Local Development

```bash
cds watch
```

Open http://localhost:4004 to access the application.

## Project Structure

```
{{ project_id }}/
├── db/
│   └── schema.cds          # Data model
├── srv/
│   ├── service.cds         # Service definition
│   ├── service.js          # Event handlers
│   └── annotations.cds     # UI annotations
├── app/
│   └── {{ main_entity | lower }}/
│       ├── webapp/
│       │   ├── manifest.json
│       │   └── Component.js
├── mta.yaml                # Deployment descriptor
└── xs-security.json        # Authorization
```

## Entities

{% for entity in entities %}
### {{ entity.name }}

{{ entity.description or '' }}

| Field | Type | Key | Nullable |
|-------|------|-----|----------|
{% for field in entity.fields %}
| {{ field.name }} | {{ field.type }} | {% if field.key %}✓{% endif %} | {% if field.nullable %}✓{% endif %} |
{% endfor %}

{% endfor %}

## API Endpoints

- `GET /{{ service_name | lower }}/{{ main_entity }}` - List all
- `GET /{{ service_name | lower }}/{{ main_entity }}({id})` - Get by ID
- `POST /{{ service_name | lower }}/{{ main_entity }}` - Create
- `PATCH /{{ service_name | lower }}/{{ main_entity }}({id})` - Update
- `DELETE /{{ service_name | lower }}/{{ main_entity }}({id})` - Delete

## Deployment

### Build

```bash
mbt build
```

### Deploy to SAP BTP

```bash
cf deploy mta_archives/{{ project_id }}_1.0.0.mtar
```

## Security

Roles:
{% for role in roles %}
- **{{ role.name }}**: {{ role.description }}
{% endfor %}

---

Generated by SAP App Builder
